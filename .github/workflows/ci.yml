name: CI
# Thin caller — all CI logic lives in the shared public reusable workflow.
# Version-pinned to @v1 so a central change can't touch this repo until the
# tag is moved. This file is this repo's last CI commit.
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  ci:
    uses: tobyrosen/oss-workflows/.github/workflows/ci.yml@664ea1ccf88efd6dae49a1d5141b733496514dd1 # v1
